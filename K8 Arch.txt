KT Session Outline: Kubernetes and Its Architecture
1. Introduction to Kubernetes
What is Kubernetes?

Kubernetes (K8s) is an open-source container orchestration platform designed to automate the deployment, scaling, and management of containerized applications.

It was originally developed by Google and is now maintained by the Cloud Native Computing Foundation (CNCF).

Why Kubernetes?

Simplifies container management.

Provides high availability, scalability, and self-healing capabilities.

Enables DevOps practices like CI/CD, rolling updates, and blue-green deployments.

2. Key Features of Kubernetes
Automated Scheduling: Automatically places containers based on resource requirements and constraints.

Self-Healing: Restarts failed containers, replaces and reschedules containers when nodes die.

Horizontal Scaling: Automatically scales applications up or down based on CPU usage or custom metrics.

Service Discovery and Load Balancing: Automatically assigns IPs and DNS names to containers and distributes traffic.

Storage Orchestration: Automatically mounts storage systems (local, cloud, etc.).

Rolling Updates and Rollbacks: Updates applications with zero downtime and allows rollbacks if something goes wrong.

3. Kubernetes Architecture
Kubernetes follows a master-worker architecture. The main components are:

A. Control Plane (Master Node)
kube-apiserver:

The front-end for the Kubernetes control plane.

Exposes the Kubernetes API for all operations.

etcd:

A distributed key-value store used to store all cluster data (e.g., configurations, states).

kube-scheduler:

Watches for newly created pods and assigns them to nodes based on resource availability.

kube-controller-manager:

Runs controller processes (e.g., Node Controller, Replication Controller) to regulate the state of the cluster.

cloud-controller-manager (optional):

Manages cloud-specific controllers (e.g., load balancers, storage).

B. Worker Nodes
kubelet:

An agent that runs on each node and ensures containers are running in a pod.

kube-proxy:

Maintains network rules to allow communication to and from pods.

Container Runtime:

Software that runs containers (e.g., Docker, containerd, CRI-O).

C. Add-ons
DNS (CoreDNS): Provides DNS services for the cluster.

Dashboard: Web-based UI for managing the cluster.

Ingress Controller: Manages external access to services.

CNI (Container Network Interface): Provides networking for pods (e.g., Calico, Flannel).

4. Kubernetes Objects
Pods:

The smallest deployable unit in Kubernetes.

Contains one or more containers that share storage and network.

Deployments:

Manages stateless applications.

Ensures a specified number of pod replicas are running.

Services:

Exposes pods to the network (ClusterIP, NodePort, LoadBalancer).

ConfigMaps and Secrets:

Store configuration data and sensitive information, respectively.

Persistent Volumes (PVs) and Persistent Volume Claims (PVCs):

Manage storage for stateful applications.

Namespaces:

Logical partitions for organizing resources in a cluster.

5. Kubernetes Workflow
Step 1: Define the desired state of your application using YAML/JSON manifests.

Step 2: Submit the manifest to the Kubernetes API server.

Step 3: The scheduler assigns pods to nodes.

Step 4: The kubelet on each node ensures the containers are running.

Step 5: Controllers continuously monitor and maintain the desired state.

6. Demo (Optional)
Deploy a Simple Application:

Create a Deployment YAML file.

Expose the application using a Service.

Show how to scale the application.

Basic Commands:

kubectl get pods

kubectl apply -f <file.yaml>

kubectl scale deployment <deployment-name> --replicas=3


=======================================================================================================================================

Here's a brief explanation of key Kubernetes (K8s) concepts with examples:

1️⃣ Namespace
A logical separation within a Kubernetes cluster to organize resources.
Used to create isolated environments (e.g., dev, test, prod).
Example: Create a Namespace

yaml
Copy
Edit
apiVersion: v1
kind: Namespace
metadata:
  name: my-namespace
Apply it:

sh
Copy
Edit
kubectl apply -f namespace.yaml
2️⃣ Node
A worker machine (physical/virtual) in a K8s cluster.
Runs Pods and is managed by the Kubelet.
Example: List Nodes

sh
Copy
Edit
kubectl get nodes
3️⃣ Pod
The smallest deployable unit in K8s.
Contains one or more containers that share storage/network.
Example: Pod Definition

yaml
Copy
Edit
apiVersion: v1
kind: Pod
metadata:
  name: my-pod
spec:
  containers:
    - name: nginx-container
      image: nginx
Apply it:

sh
Copy
Edit
kubectl apply -f pod.yaml
4️⃣ Deployment
Manages replicas and ensures Pods are running as expected.
Supports scaling, rolling updates, and self-healing.
Example: Deployment

yaml
Copy
Edit
apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-deployment
spec:
  replicas: 3
  selector:
    matchLabels:
      app: my-app
  template:
    metadata:
      labels:
        app: my-app
    spec:
      containers:
        - name: nginx
          image: nginx:latest
Apply it:

sh
Copy
Edit
kubectl apply -f deployment.yaml
5️⃣ Service
Exposes a set of Pods internally or externally.
Types: ClusterIP (default), NodePort, LoadBalancer.
Example: Service (ClusterIP)

yaml
Copy
Edit
apiVersion: v1
kind: Service
metadata:
  name: my-service
spec:
  selector:
    app: my-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
Apply it:

sh
Copy
Edit
kubectl apply -f service.yaml
6️⃣ ConfigMap
Stores non-sensitive configuration data (e.g., env variables, config files).
Helps keep configurations separate from the application code.
Example: ConfigMap

yaml
Copy
Edit
apiVersion: v1
kind: ConfigMap
metadata:
  name: my-config
data:
  APP_ENV: "production"
  LOG_LEVEL: "debug"
Apply it:

sh
Copy
Edit
kubectl apply -f configmap.yaml
7️⃣ Secret
Stores sensitive data (e.g., passwords, API keys, certificates).
Data is base64 encoded (not encrypted).
Example: Secret

yaml
Copy
Edit
apiVersion: v1
kind: Secret
metadata:
  name: my-secret
type: Opaque
data:
  DB_PASSWORD: bXlTZWNyZXRQYXNz (base64 encoded)
Apply it:

sh
Copy
Edit
kubectl apply -f secret.yaml
8️⃣ Ingress
Manages external access to services using HTTP/HTTPS.
Supports path-based routing, TLS termination, and load balancing.
Example: Ingress

yaml
Copy
Edit
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: my-ingress
spec:
  rules:
    - host: myapp.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: my-service
                port:
                  number: 80
Apply it:

sh
Copy
Edit
kubectl apply -f ingress.yaml
🔥 Summary Table
Concept	Purpose
Namespace	Logical grouping of resources
Node	Worker machine in a cluster
Pod	Smallest deployable unit in K8s
Deployment	Manages replicas and rolling updates
Service	Exposes Pods internally or externally
ConfigMap	Stores non-sensitive config data
Secret	Stores sensitive credentials securely
Ingress	Manages external HTTP/HTTPS access





############
Here is a single Kubernetes manifest file that contains a deployment, services, ingress, secret, configmap, and persistent volume (PV):

```
apiVersion: v1
kind: List
items:
  - apiVersion: apps/v1
    kind: Deployment
    metadata:
      name: example-deployment
    spec:
      replicas: 3
      selector:
        matchLabels:
          app: example
      template:
        metadata:
          labels:
            app: example
        spec:
          containers:
          - name: example-container
            image: example/image
            ports:
            - containerPort: 80
            volumeMounts:
            - name: example-pvc
              mountPath: /mnt/data
          volumes:
          - name: example-pvc
            persistentVolumeClaim:
              claimName: example-pvc

  - apiVersion: v1
    kind: Service
    metadata:
      name: example-service
    spec:
      selector:
        app: example
      ports:
      - name: http
        port: 80
        targetPort: 80
      type: ClusterIP

  - apiVersion: networking.k8s.io/v1
    kind: Ingress
    metadata:
      name: example-ingress
    spec:
      rules:
      - host: example.com
        http:
          paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: example-service
                port:
                  number: 80

  - apiVersion: v1
    kind: Secret
    metadata:
      name: example-secret
    type: Opaque
    data:
      username: <base64 encoded username>
      password: <base64 encoded password>

  - apiVersion: v1
    kind: ConfigMap
    metadata:
      name: example-configmap
    data:
      config.properties: |
        key1=value1
        key2=value2

  - apiVersion: v1
    kind: PersistentVolume
    metadata:
      name: example-pv
    spec:
      capacity:
        storage: 1Gi
      accessModes:
        - ReadWriteOnce
      persistentVolumeReclaimPolicy: Retain
      local:
        path: /mnt/data
      storageClassName: local-storage

  - apiVersion: v1
    kind: PersistentVolumeClaim
    metadata:
      name: example-pvc
    spec:
      accessModes:
        - ReadWriteOnce
      resources:
        requests:
          storage: 1Gi
```

This manifest file defines the following resources:

- A deployment named `example-deployment` with 3 replicas.
- A service named `example-service` that selects the `example-deployment` pods.
- An ingress named `example-ingress` that routes traffic to the `example-service` service.
- A secret named `example-secret` that stores a username and password.
- A configmap named `example-configmap` that stores a configuration file.
- A persistent volume (PV) named `example-pv` that provides 1Gi of storage.
- A persistent volume claim (PVC) named `example-pvc` that requests 1Gi of storage.

Note that you will need to replace the `<base64 encoded username>` and `<base64 encoded password>` placeholders in the secret with the actual base64 encoded values. Additionally, you may need to modify the storage class and local path in the PV definition to match your specific environment.
